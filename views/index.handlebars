<div class="container py-5">

  <div class="row">
    <div class="col-12 col-lg-10 col-xl-8 mx-auto">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">隨機密碼生成器 <i class="fas fa-key"></i></h5>
          <hr>
          <form method="POST" action="/">
            <div class="form-group form-row">
              <label for="length" class="col-md-4 col-form-label">
                密碼長度
              </label>
              <div class="col-md-8">
                <input type="number" name="length" class="form-control" id="length" min="4" max="16"
                  value="{{options.length}}">
                <small class="text-muted form-text">長度請介於4~16之間</small>
              </div>
            </div>

            <div class="form-group form-row mb-2 align-items-center">
              <label for="lowercase" class="col-md-4 col-form-label">包含小寫字母</label>
              <div class="col-md-8">
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="lowercase" name="lowercase" {{#if
                    options.lowercase}} checked {{/if}}>
                  <label class="custom-control-label" for="lowercase">(e.g. abcdefg)</label>
                </div>
              </div>
            </div>

            <div class="form-group form-row mb-2 align-items-center">
              <label for="uppercase" class="col-md-4 col-form-label">包含大寫字母</label>
              <div class="col-md-8">
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="uppercase" name="uppercase" {{#if
                    options.uppercase}} checked {{/if}}>
                  <label class="custom-control-label" for="uppercase">(e.g. ABCDEFG)</label>
                </div>
              </div>
            </div>

            <div class="form-group form-row mb-2 align-items-center">
              <label for="numbers" class="col-md-4 col-form-label">包含數字</label>
              <div class="col-md-8">
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="numbers" name="numbers" {{#if
                    options.numbers}} checked {{/if}}>
                  <label class="custom-control-label" for="numbers">(e.g. 123456)</label>
                </div>
              </div>
            </div>

            <div class="form-group form-row mb-2 align-items-center">
              <label for="symbols" class="col-md-4 col-form-label">包含符號</label>
              <div class="col-md-8">
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input" id="symbols" name="symbols" {{#if
                    options.symbols}} checked {{/if}}>
                  <label class="custom-control-label" for="symbols">(e.g. @#$%)</label>
                </div>
              </div>
            </div>

            <div class="form-group form-row">
              <label for="exclude-characters" class="col-md-4 col-form-label">
                排除的字符
              </label>
              <div class="col-md-8">
                <input type="text" name="excludeCharacters" class="form-control" id="exclude-characters"
                  value="{{options.excludeCharacters}}">
                <small class="text-muted form-text">字符將不會出現在密碼裡</small>
              </div>
            </div>

            {{#if password}}
            <div class="alert alert-success" role="alert">
              <span class="mr-2">您的密碼是:</span><code>{{password}}</code>
            </div>
            {{/if}}

            <button class="btn btn-success" type="submit">隨機產生</button>
          </form>
        </div>

        <small class="card-footer text-muted text-right">
          Idea is inspired by <a href="https://passwordsgenerator.net/" target="_blank">Strong Random Password</a>
        </small>
      </div>
    </div>
  </div>
</div>